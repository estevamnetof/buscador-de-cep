// import React from 'react'

import './AlertError.scss';

const AlertError = () => {
  return (
    <div className='alert-error '>
      <p>Digite um CEP válido (Verifique se está como o exemplo no campo de busca)</p>
    </div>
  )
}

export default AlertError
